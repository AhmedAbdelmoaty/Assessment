You are The Ultimate Full-Stack Engineer (Node.js + Express + Vanilla HTML/CSS/JS) and AI Prompt/Context Architect.
You must deliver production-grade, minimal-dependency, cleanly-architected features that slot perfectly into an existing Express + vanilla frontend app.
Your priorities: correctness → clarity → maintainability → performance.
Do not invent new pages or colors. Do not run tests. Implement exactly what is described.

🧩 PROJECT CONTEXT (EXISTING)

Stack: Express (ESM) backend, vanilla HTML/CSS/JS frontend (no React/SPA frameworks), no DB today (we will add Postgres).

Current flow: Landing → Intake (collects user info) → Assessment L1→L3 (2 MCQs/level) → Final Report → Teaching mode (chat tutor).

Files of interest (already exist, do not rename):

server/index.js — all routes and logic are here now.

server/prompts/system.js — MCQ generator (system prompt).

server/prompts/report.js — final report prompt.

server/prompts/teach.js — teaching system prompt.

server/shared/topicDisplayMap.js

public/index.html + public/styles.css + public/app.js

Design constraint: keep the current look & colors (IMP brand maroon etc.) from styles.css.
UX constraint: ALL auth (signup, OTP, set password, login) happens inside the chat UI (as inline blocks), not standalone pages.
We do add one new page: /dashboard (same styling), everything else stays in public/index.html.

🎯 WHAT TO BUILD (TOP-LEVEL)

PostgreSQL persistence

Migrate from in-memory session and progress to DB-backed (Postgres + connect-pg-simple).

Store users, OTPs, sessions, progress, and assessment history.

Inline Auth in Chat (no standalone pages)

First-time visit: go through Intake as today, move email to be the last Intake question.

After Intake is done:

Create/update a user record from name/email.

Generate a 6-digit OTP (valid 15 min), store in DB.

If SMTP env exists: send email with OTP.
If SMTP env missing: DEV-mode → print OTP in server logs and show a small [DEV ONLY] bubble in chat.

Show an inline OTP input block in the chat; on success, show an inline Set Password block (first-time).

After password is set → user is authenticated (session cookie) → begin Assessment.

Returning Users
On load, call /api/auth/me. Depending on server state (via DB):

If in middle of Intake → resume last Intake question.

If Intake complete but email not verified → show OTP block immediately.

If verified and in Assessment → resume next question.

If finished and at Report/Teaching → resume there.
Do not re-ask Intake unless the user explicitly resets later.

Dashboard Page (/dashboard)
A simple vanilla page (same styles):

My Info: show Intake data (editable except email). Save on submit.

My Assessments: list of past attempts with percentage score + simple progress bars.

My Tutorials: list the topics the tutor covered (titles + timestamps).

My Videos: placeholder section for future content.

Link to /dashboard appears only when logged-in (you can add a small nav link in the header).

“Retake Assessment (Harder)”
After a completed assessment/report, show a Retake CTA in chat: starts a fresh assessment with slightly harder variations and produce a percentage score at the end. Always store each attempt in history.

Security/Resilience

Sessions: express-session + connect-pg-simple.

Passwords: bcrypt.

Rate-limit OTP endpoints.

helmet for security headers.

Zod validation for all auth inputs.

Do not print OTP in production. Only in DEV-mode (no SMTP).

Do not run tests. Only implement.

🗃️ DATABASE (Postgres) — Tables

Use Drizzle or SQL via pg (Drizzle preferred since it’s already listed). Create a migrations folder.

users

id (pk, uuid)

email (text, unique, indexed)

name (text)

password_hash (text, nullable until first set)

email_verified_at (timestamptz, nullable)

created_at (timestamptz, default now)

updated_at (timestamptz, default now)

auth_otps

id (pk, uuid)

user_id (fk → users.id)

code (text, 6 digits)

expires_at (timestamptz)

consumed_at (timestamptz, nullable)

created_at (timestamptz)

user_sessions (used by connect-pg-simple; use the default schema it expects, or create table session with sid, sess, expire)

user_progress

id (pk, uuid)

user_id (fk)

intake (jsonb) // answers so far

flow_state (text) // 'intake' | 'otp_pending' | 'set_password' | 'assessment' | 'report' | 'teaching'

assessment_state (jsonb) // copy what’s now in memory: currentLevel, attempts, evidence, etc.

created_at, updated_at

user_assessments

id (pk, uuid)

user_id (fk)

started_at, completed_at (timestamptz)

difficulty (text) // 'normal' | 'harder'

score_percent (int) // 0..100

evidence (jsonb) // full history for that attempt

🔐 ENV & SMTP

Add support for SMTP via nodemailer:

SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, SMTP_FROM

If all present → send real email with OTP.

If missing → DEV-mode: log OTP to server console and also send a chat bubble tag [DEV ONLY] Your OTP is 123456.

Also:

SESSION_SECRET (required)

DATABASE_URL (Postgres)

Call app.set('trust proxy', 1).

🔗 API CONTRACTS (add to server/index.js)

Insert the new middleware and routes at precise anchors below.

0) Session & Security Middleware

File: server/index.js
Insert after:

const app = express();
app.use(express.json());
app.use(express.static(join(__dirname, "../public")));


Insert this block (BEFORE any routes):

// === Security & Sessions (insert here) ===
import session from "express-session";
import pgSession from "connect-pg-simple";
import helmet from "helmet";
import rateLimit from "express-rate-limit";

app.set("trust proxy", 1);
app.use(helmet());

const PgSession = pgSession(session);
app.use(session({
  store: new PgSession({ conString: process.env.DATABASE_URL }),
  secret: process.env.SESSION_SECRET || "dev_secret_change_me",
  resave: false,
  saveUninitialized: false,
  cookie: {
    httpOnly: true,
    sameSite: "lax",
    secure: process.env.NODE_ENV === "production",
    maxAge: 1000 * 60 * 60 * 24 * 30 // 30 days
  }
}));

const otpLimiter = rateLimit({ windowMs: 15 * 60 * 1000, max: 10 });

1) Auth + Intake Orchestration
POST /api/intake/next (MODIFY)

Move “email” to be the final Intake step (ensure INTAKE_ORDER in server/index.js ends with email).

After the last Intake answer is saved:

Upsert users by email + name.

Upsert user_progress with { intake, flow_state: 'otp_pending' }.

Generate OTP(6 digits/15min) → save auth_otps.

If SMTP configured → send. Else DEV-mode (log & chat bubble).

Return JSON to client instructing it to render OTP inline block.

Where to change:

File: server/index.js

Section: existing /api/intake/next handler

Insert after it detects session.intakeStepIndex >= INTAKE_ORDER.length and BEFORE it switches to assessment.

Replace the “We’ll start the assessment now.” message with server directive to show OTP step:

return res.json({
  done: true,
  next: "otp_required",
  message: lang === "ar"
    ? "تم تجميع بياناتك. من فضلك أدخل رمز التحقق المرسل إلى بريدك الإلكتروني."
    : "We’ve captured your details. Please enter the verification code sent to your email."
});

POST /api/auth/otp/request

Body: { email } (only when user re-requests)

Server: re-issue OTP, same rules (rate-limited).

POST /api/auth/otp/verify

Body: { email, code }

Server: validate OTP (match + not expired + not consumed), then:

mark consumed,

set flow_state to set_password if user has no password, else assessment,

attach session (logged-in).

Return:

{ "ok": true, "next": "set_password" | "assessment" }

POST /api/auth/set-password

Body: { password } (first time after OTP)

Server: bcrypt hash → store, set flow_state = 'assessment', ensure session.

POST /api/auth/login

Body: { email, password }

Server: bcrypt compare → if ok, ensure session; return current flow_state so UI resumes properly.

GET /api/auth/me

Return { loggedIn: boolean, flow_state, intakeProgress?, assessmentProgress? } so client resumes correctly.

POST /api/auth/logout

Destroy session.

Where to paste auth routes:

File: server/index.js

Insert before the Assessment routes.

Anchor to insert before:
Search for:
// -------- Assessment: get ONE MCQ --------
Insert the new “Auth Routes” block right above it, starting with:

/* =========================
   Auth Routes (OTP + Password)
   ========================= */

2) Persist Assessment & Resume

Replace all in-memory sessions usage related to assessment progress with DB-backed user_progress for the logged-in user.

On each question serve/answer:

Load progress from DB,

Update assessment_state & flow_state as needed,

Save back to DB.

At report generation:

Compute percentage score = (total_correct / total_questions) * 100 (round to int),

Save a row in user_assessments with difficulty ('normal' by default, 'harder' for retakes).

Where: inside the existing routes /api/assess/next, /api/assess/answer, /api/report.
Do not change the prompt logic; only move state persistence to DB and return the same response schema to the client.

3) Retake (Harder)

After /api/report returns, include an extra flag { canRetake: true }.

Add a new route POST /api/assess/retake:

Reset assessment_state to initial,

Mark an internal flag difficulty='harder' for this attempt,

Set flow_state='assessment'.

Client: show a CTA button in the final report bubble. On click, call /api/assess/retake then start /api/assess/next.

4) Dashboard

Add public/dashboard.html and public/dashboard.js.

Re-use styles.css (do not change brand colors).

Sections (4) laid out in a simple responsive grid:

My Info: editable form (name, age_band, job_nature, …) except email (read-only). POST to /api/user/profile.

My Assessments: GET /api/user/assessments (list attempts with date + score%). Render progress bars (simple CSS).

My Tutorials: GET /api/user/tutorials (you can reuse teaching transcripts already saved or add minimal storage: keep topic names + timestamps in user_progress or a small user_tutorials table).

My Videos: placeholder card (“Coming soon…”).

Add routes:

GET /api/user/profile

POST /api/user/profile (update; do not allow email change)

GET /api/user/assessments

GET /api/user/tutorials (if tutorials are not persisted yet, return empty array for now)

Where to serve page:

server/index.js
Before the SPA fallback, add:

app.get("/dashboard", (req, res) => {
  if (!req.session?.userId) return res.redirect("/");
  res.sendFile(join(__dirname, "../public/dashboard.html"));
});

🖥️ FRONTEND (minimal, vanilla)
A) public/app.js — Modify

Language toggle / general UI: keep as-is.

Intake flow:

Ensure email question is last.

When /api/intake/next returns { done: true, next: "otp_required" }:

Render inline OTP block in chat (6 inputs or one input with mask).

Buttons: Verify → POST /api/auth/otp/verify, Resend → POST /api/auth/otp/request.

DEV-mode: if server included dev_otp in response, show a small bubble [DEV ONLY] Your OTP is 123456.

If verify returns next: "set_password":

Render inline Set Password block in chat.

On success → begin assessment as today.

Auto-resume on load:

Call /api/auth/me:

If { loggedIn: true }: switch based on flow_state:

intake → continue Intake.

otp_pending → show OTP block.

set_password → show Set Password block.

assessment → call /api/assess/next.

report or teaching → show respective UI.

If { loggedIn: false }: show a small “Log in” button in header; clicking it opens an inline login block (email+password) inside chat.

Final report:

After showing report message, add a Retake (Harder) CTA button below (same style as existing CTA buttons). On click → POST /api/assess/retake then restart assessment.

Design:

Do NOT change branding/colors.

Reuse existing CSS utility classes.

OTP/Password blocks should visually match existing chips/cards.

B) public/dashboard.html + public/dashboard.js

Simple header with brand logo, “Back to Chat” link (/), and a small “Logout” button (calls /api/auth/logout and redirects to /).

Four sections with headings that match typography in styles.css.

Fetch profile/assessments/tutorials from the endpoints above.

Bars for scores: simple rounded div with width = ${score}%.

🔒 VALIDATION & EDGE CASES

OTP: 6 numeric digits, expires in 15 minutes; one-time use. Rate-limit /otp/request and /otp/verify.

Password: min 8 chars; bcrypt hash.

Session cookie: secure in prod, httpOnly: true, sameSite: 'lax'.

Do not expose OTP in production.

If SMTP env present, send real email via nodemailer; else DEV-mode bubble+log.

📍 EXACT “WHERE TO PASTE” SUMMARY

server/index.js

After static + JSON middleware → insert Security & Sessions block.

Before // -------- Assessment: get ONE MCQ -------- → insert “Auth Routes (OTP + Password)” block and user/profile endpoints.

Modify /api/intake/next so that after last question → set DB state to otp_pending, generate/send OTP, and return { done:true, next:"otp_required" } (no assessment start yet).

Persist assessment/report state to DB (user_progress) inside /api/assess/next, /api/assess/answer, /api/report.

Add POST /api/assess/retake.

Add app.get("/dashboard", ...) before SPA fallback.

public/app.js

At start, call /api/auth/me and route based on flow_state.

After Intake completion → show OTP block; after verify → Set Password block; then assessment.

Add inline login block (email+password) inside chat for logged-out users.

In final report message render → append Retake (Harder) CTA and wire to /api/assess/retake.

public/dashboard.html & public/dashboard.js

New files with sections & fetch calls; same CSS branding.

🚫 DO NOT

Do not create separate /login or /signup pages.

Do not change colors/brand.

Do not refactor prompts.

Do not remove existing routes.

Do not run tests.

Do not introduce React/TS/DB other than Postgres.

✅ DONE WHEN

I can complete Intake, get OTP in DEV bubble/log (or real email if SMTP set), verify, set password, and start Assessment — all inside chat.

On refresh, I’m resumed to my exact place.

I can visit /dashboard (when logged-in) and see:

My Info (editable),

My Assessments (with percentages + bars),

My Tutorials (or empty),

My Videos (placeholder).

After a report, I see Retake (Harder); clicking it starts a harder run and saves a percentage score in history.

Implement exactly as specified above.
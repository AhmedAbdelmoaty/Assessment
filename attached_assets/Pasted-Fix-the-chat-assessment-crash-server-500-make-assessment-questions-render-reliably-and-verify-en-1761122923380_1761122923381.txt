Fix the chat assessment crash (server 500), make assessment questions render reliably, and verify end-to-end persistence (intake → assessment → teaching) is reflected on both dashboards.

Repro right now

Log in with a verified user that has completed intake.

Open /chat.html. You’ll see “Welcome back! We’ll start the assessment directly.” then “Sorry, an error occurred during assessment.”

Browser console is empty; the issue is server-side in POST /api/assess/next.

Do this:

1) Instrument & diagnose /api/assess/next

Add temporary dev logging around the OpenAI call and before returning errors (guard by NODE_ENV !== 'production'):

Log: incoming sessionId, session.currentStep, assessment state, model name, and the first 200 chars of systemPrompt.

If OpenAI throws, log status, code, and raw error body.

On error, return JSON like: { error:true, stage:"assess-next", reason:"<short>", detail:"<message or code>" } so the frontend can show a friendly hint during dev.

Files to inspect/edit:
server/index.js → the handlers for /api/assess/next, /api/assess/answer.

2) Make the OpenAI call robust

Ensure the API key is read from one env var (OPENAI_API_KEY) and warn if absent.

Use an available model (e.g. "gpt-4o-mini") with response_format: { type: "json_object" }. Remove any "gpt-5" references in fallback paths.

After JSON.parse, validate the schema strictly:

kind === "question"

Array.isArray(choices) and choices.length >= 3

integer correct_index, 0 <= correct_index < choices.length

If invalid or the call fails, fallback to a minimal hardcoded MCQ for the current level so the flow never dies. Log that the fallback path was used.

3) Verify session/flow guards

When intake finishes, we set session.currentStep = "assessment"; verify it persists in the in-memory sessions map.

Confirm the same sessionId is sent by the frontend for /api/assess/next and /api/assess/answer. If session.currentStep !== "assessment" or !session.assessment, initialize it and continue.

Keep the 6-question scoring rule (2 per level; only the latest retry counts; incomplete = not saved).

4) Frontend: surface useful errors during dev

In public/chat.js (or equivalent), if /api/assess/next returns { error:true, ... } in dev, show a soft banner with reason (not raw stack). In production, keep the generic message.

5) End-to-end verification

Complete an assessment fully. Confirm the backend writes one row to attempts with:

total_questions = 6, correct_answers, score_percent, finished_at not null.

Confirm user dashboard shows:

Intake values from users.profile_json.intake

The new completed assessment (score & date)

Saved explanations (if any)

Confirm admin dashboard shows:

Real counts and the pivot distributions based on persisted intake

Users table rows with intake columns populated

6) Deliverables

Paste the /api/assess/next server log excerpt for:

One successful OpenAI path or

One fallback path (and why it fell back)

Brief list of files edited and a one-line note per change.

A short test script (curl or HTTPie) that hits /api/assess/next with a valid sessionId to prove it returns a valid MCQ JSON.

Constraints

No schema changes; the current tables/columns are correct.

Keep RBAC and routes as-is.

Hide verbose error details when NODE_ENV === 'production'.

Quick manual checks you can do after the agent runs

Network tab on /chat.html → open /api/assess/next response. It should be:

{
  "kind":"question",
  "level":"L1",
  "cluster":"central_tendency_foundations",
  "prompt":"...",
  "choices":["A","B","C","D"],
  "questionNumber":1,
  "totalQuestions":2
}


Finish the flow → in admin dashboard, refresh the pivot and users table; in user dashboard, you should see the new assessment row with the score.

If OpenAI key is missing or rate-limited, you should still get a fallback MCQ and be able to complete.
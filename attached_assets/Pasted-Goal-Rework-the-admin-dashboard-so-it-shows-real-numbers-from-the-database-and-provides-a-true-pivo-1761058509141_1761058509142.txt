Goal: Rework the admin dashboard so it shows real numbers from the database and provides a true pivot matrix (rows/columns/filters) + a synced users table. No mock data, no nested tree drilldowns. Keep it simple and responsive.

0) Access control

Only users with role admin may access /admin.html and any /api/admin/* endpoints.

Add/ensure a requireAdmin middleware. Non-admins → 401 for APIs and redirect to /login.html for /admin.html.

Admin should not see user pages; users should not see admin pages.

1) Data endpoint (raw rows, flat shape)

Create GET /api/admin/users/raw (protected by requireAdmin) that returns flat rows pulled from users:

Source: users table. Fields:

id, email, username

From users.profile_json:

name (top level under profile_json)

From profile_json.intake:

country

age_band

sector

job_nature

experience_years_band

learning_reason

Do NOT include createdAt.

Keep empty values as null or empty string (no fake defaults).

Example row:

{
  "id": "uuid",
  "email": "x@y.com",
  "username": "user1",
  "name": "Ahmed Ali",
  "country": "Egypt",
  "age_band": "25–34",
  "sector": "Finance",
  "job_nature": "IT/Data",
  "experience_years_band": "3–5y",
  "learning_reason": "Promotion"
}

2) Admin UI (pivot + table)

Update public/admin.html (and a small public/js/admin.js) to:

Controls (top bar)

Rows: single-select dropdown

Columns: single-select dropdown with option “None”

Filters: multi-select for any of the fields below

Refresh button (re-fetch /api/admin/users/raw)

Available dimensions: country, sector, job_nature, age_band, experience_years_band, learning_reason.

Pivot Matrix

Build the matrix on the client from the raw rows.

Cell = count of users.

Show Row totals, Column totals, and Grand total.

If Columns = “None”, show a single-count column grouped by Rows.

Optional: lightweight heatmap coloring for readability.

No fixed nested drilldown; it must be a true pivot (I can swap Rows/Columns freely).

Users Table (synced with filters)

Below the pivot, show a table with columns:
Name, Email, Username, Country, Age Band, Sector, Job Nature, Experience, Reason

The table responds to Filters (Rows/Columns don’t filter it).

Include quick text search (name/email/username) and simple pagination.

Optional: “Export CSV”.

Simple KPIs (optional)

Card: Total Users (from the fetched data, before applying Filters)

Card: Filtered Users (after Filters)

Card: Top Country (after Filters)

3) Remove any fake numbers

Delete any hardcoded counts in admin.html or related scripts. All figures must derive from /api/admin/users/raw.

4) Performance notes

Keep the raw dataset in memory client-side; rebuild the pivot instantly when Rows/Columns/Filters change.

Debounce filter changes.

5) Acceptance checks

Changing Rows/Columns/Filters updates the pivot immediately without reloading.

Pivot totals match the DB when verified with SQL (examples below).

Users table shows only the rows that match current Filters.

Non-admins can’t access /admin.html or /api/admin/*.

SQL to verify correctness

Total users:

SELECT COUNT(*) FROM users;


Count by country:

SELECT NULLIF(TRIM(profile_json->'intake'->>'country'), '') AS country,
       COUNT(*) AS users_count
FROM users
GROUP BY 1
ORDER BY users_count DESC NULLS LAST;


Country × Sector:

SELECT NULLIF(TRIM(profile_json->'intake'->>'country'), '') AS country,
       NULLIF(TRIM(profile_json->'intake'->>'sector'), '')  AS sector,
       COUNT(*) AS users_count
FROM users
GROUP BY 1,2
ORDER BY country NULLS LAST, users_count DESC;


Important: No mock data, no default filler values, no nested JSON in the API response; the shape must be flat exactly as specified and without createdAt.

If you want, I can also give you a one-liner to ask the Agent to leave the SQL snippets in comments inside the PR for quick manual verification.
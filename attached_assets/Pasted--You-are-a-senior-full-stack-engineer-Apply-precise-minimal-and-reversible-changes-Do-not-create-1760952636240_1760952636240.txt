
You are a senior full-stack engineer. Apply precise, minimal, and reversible changes. Do not create new pages, do not refactor unrelated code, and do not touch OTP logic. Fix only the password-setting flow that currently throws a Postgres syntax error.

Context

Stack: Node.js + Express, Drizzle ORM (PostgreSQL via @neondatabase/serverless), bcrypt, sessions stored in Postgres.

Current UI shows: “Failed to set password”.

Server console shows (repeatable):
NeonDbError: syntax error at or near "where" originating from server/index.js around the password-set handler (approx lines ~560–590).

Recent schema renames: passwordHash → passHash, name → username.

OTP flow is now working after rollback. Do not edit OTP logic.

Goal
Fix the Set Password endpoint only. Use Drizzle’s update builder correctly and the correct column names. Ensure the SQL becomes valid (no WHERE before SET), the hash field is passHash, and the selected user row is guaranteed (via userId from session or verified email). Return a clear JSON response on success/failure.

Exact Tasks

Locate the password-set route/handler (in server/index.js or server/auth.js depending on current code).

Ensure it:

Validates input: password length ≥ 6.

Uses bcrypt.hash(password, 10) to get hash.

Updates the correct row with Drizzle, e.g.:

import { eq } from "drizzle-orm";
// users table has column: passHash (NOT passwordHash)
await db
  .update(users)
  .set({ passHash: hash /*, updatedAt: new Date() if column exists */ })
  .where(eq(users.id, userId)); // or eq(users.email, email) if that’s how you identify the user here


Do not use raw SQL strings that might invert the order (WHERE before SET).

Make sure the handler:

Reads the user target from a reliable source (session req.session.userId if present; otherwise the validated email used during OTP verification).

Returns { ok: true } on success; { ok: false, error: "..." } on failure.

Logs a concise error with the user identifier but never logs the raw password or the hash.

Do not modify OTP code. Do not rename columns again. Do not introduce new routes or files. Keep styles and UI intact.

Add one minimal debug log near the update to print only { userId, updated: <rowCount> }.

Acceptance Criteria

Submitting the password in the chat form no longer shows “Failed to set password”.

Server log no longer prints syntax error at or near "where".

Database gets the passHash updated for the correct user.

OTP flow remains unchanged and functional.

No new pages, no UI restyling, no unrelated edits.

Note
If the handler still references passwordHash, change it to passHash. If it uses raw SQL, replace with Drizzle builder. Do not touch any OTP code or intake/report flows.

إزاي تتأكد بعد الإصلاح؟

امشي التدفق لحد شاشة “اختَر كلمة مرور”.

جرّب كلمة ≥ 6 أحرف → المفروض يرد { ok: true } ويكمل من غير popup error.

راقب اللوج: تشوف سطر زي:
set-password ok { userId: '...', updated: 1 }

افتح الـDB (لو تقدر) وتأكد إن passHash اتحدّث.

لو حبيت أبعث للـAgent رسالة أقصر “follow-up” للحماية من العبث بالـOTP:

“OTP flow is working after rollback. Please do not change any OTP code again. Only fix the Set Password UPDATE using Drizzle and the passHash column. The current error is syntax error at or near "where".”
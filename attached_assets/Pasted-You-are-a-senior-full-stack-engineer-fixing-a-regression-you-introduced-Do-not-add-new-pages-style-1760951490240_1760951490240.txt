You are a senior full-stack engineer fixing a regression you introduced. Do not add new pages, styles, or flows. Only patch the backend logic and any minimal client calls needed for correctness. Keep all existing UX inside the chat page.

CONTEXT (Regression)

Before your last change (to the Set Password flow), OTP verification was working.

After your change, entering a fresh 6-digit OTP now returns “رمز خاطئ أو منتهي الصلاحية” (“invalid/expired”) even when the code is correct and just generated.

I need you to repair OTP verification without breaking anything else, then ensure the flow proceeds to Set Password and succeeds.

WHAT TO FIX

OTP Verify must work again

Read the email to verify from the session (e.g., req.session.pendingEmail) or the last intake email (no reliance on a finalized userId yet).

Query the DB for the latest, unused OTP for that email:

SELECT * FROM otps
WHERE email = $1 AND used = false
ORDER BY created_at DESC
LIMIT 1;


Compare the input as a string (no parseInt, preserve leading zeros).

Check expiration in UTC (now < expires_at).

If valid:

UPDATE otps SET used=true WHERE id=$otpId;

UPDATE users SET is_email_verified=true WHERE email=$email;

req.session.userId_pending = user.id; await req.session.save();

Return { ok: true, next: "set_password" }.

If invalid: return { ok:false, error:"INVALID_OTP" } (don’t silently resend).

Do not change UI or add new pages

Keep the current chat-embedded OTP form and Set Password form.

Do not alter styles/branding.

Fix Set Password write (if needed)

Ensure it updates the correct user row using req.session.userId_pending (or the verified email → user.id), hashing the password into passHash (or the existing hash column).

Clear userId_pending after success, and keep the normal login session.

Schema/Name alignment

Make sure field and table names match the current Drizzle schema (e.g., code vs otpCode, expires_at vs expiresAt).

If you renamed fields during your last change, revert or adapt queries accordingly.

Dev logging only

Add a temporary debug log around OTP verify:

console.log("otp-verify debug", { email, input: String(code), found: !!row, codeLen: row?.code?.length, expiresAt: row?.expires_at });


Do not spam logs elsewhere. No tests or long test runs.

ACCEPTANCE (Manual)

Generate OTP (dev mode prints code to server logs). Enter it immediately → verification succeeds, UI advances to Set Password.

Enter a valid new password → password saved (no popups like “Failed to set password”).

Re-enter the same OTP → should be invalid (marked used).

A code starting with 0 verifies correctly (no numeric cast issues).

No UI/route additions, no design changes.

FILES TO TOUCH (likely)

server/routes/auth.js (or the auth routes you created)

Any OTP service/module and the Drizzle query code you wrote

Minimal client call adjustments if the response shape changed during your last edit

Important: This is a regression fix. Keep the scope surgical. Do not introduce new flows or pages.
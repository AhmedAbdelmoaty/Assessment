ROLE: You are a senior full-stack engineer working on a Vanilla JS + Express app. Follow all instructions precisely. Do not create extra pages or styles that don’t match the existing brand. No test runs—apply the changes only.

GOAL: Fix authentication and assessment flow, keep login outside the chat, keep first-time sign-up inside the chat after intake, and restore assessment start. Maintain existing visual style (IMP brand) in all new UI.

A) Home page buttons & copy

In public/index.html, change the CTA text from “Start Assessment” to “Sign Up” and Arabic from “ابدأ التقييم” إلى “إنشاء حساب”. Keep same classes/brand styling.

Add a persistent Login button in the header (it already exists). It must navigate to /login.html (not refresh the same page).

B) Create a dedicated login page (outside chat)

Add public/login.html (brand-consistent: same fonts/colors/spacings from styles.css):

Fields: email, password.

Submit to POST /api/auth/login via fetch.

On success: redirect to / and immediately resume the user’s last state (see Section D).

Include a subtle link “Create account” that navigates back to / (the chat sign-up flow).

Wire the Login button in index.html to navigate to /login.html.

C) Intake + Sign-Up inside chat (first time only)

When the intake reaches the email question:

Check if email exists:

If exists: return a chat message:
EN: “This email already has an account. Please log in.”
AR: “هذا البريد مرتبط بحساب. من فضلك سجّل الدخول.”
Render a small button “Login” (opens /login.html). Do NOT send OTP here.

If not exists: proceed with OTP step (dev mode: print OTP to server logs and echo a DEV-only line in chat). After OTP verified, ask the user to set a password (inside chat). Then mark user as verified + registered.

Enforce unique email at DB level and API level:

Add a unique index on users.email if missing.

The sign-up endpoint must return 409 on duplicate email with a clear message.

Endpoints involved (server)

POST /api/auth/login → accepts email, password, validates with bcrypt, creates session in PG store, returns { ok: true }.

POST /api/auth/logout → destroys session.

Existing OTP endpoints stay as-is, but when email belongs to an existing user, never issue an OTP from intake—only point to login.

D) Session resume on app load

Add GET /api/session/state that returns the authenticated user’s progress state:

{
  "auth": true|false,
  "intakeStatus": "not-started" | "in-progress" | "complete",
  "emailVerified": true|false,
  "assessmentStatus": "not-started" | "in-progress" | "complete",
  "teachingStatus": "not-started" | "in-progress" | "complete"
}


In public/app.js init():

Call /api/session/state.

If auth:false → show home with “Sign Up” CTA + header Login button.

If auth:true:

intakeStatus:"in-progress" → open chat and continue intake from the saved question.

intakeStatus:"complete" && emailVerified:false → open chat at OTP input immediately.

assessmentStatus:"in-progress" → open chat and continue MCQs from last question.

If report/teaching was active → resume there.

E) Fix the assessment error

The server log shows: ReferenceError: createSession is not defined in server/index.js (around the handler that transitions from intake to assessment).

Implement or re-wire a function like:

// server/services/assessService.js
export async function createOrResumeSession(userId) {
  // create record in assessment_sessions if none open
  // return session id and next question pointer
}


In the place that currently calls createSession(...), import and call createOrResumeSession(userId) instead, and use its result to start the MCQ flow. Ensure the function/file is properly imported and exported.

After password is set and user verified, the next step in chat must call this service and render the first question.

F) Keep styles consistent

Do not introduce new gradients or alien components. Reuse classes from styles.css. Forms must visually match the brand (same rounded corners, fonts, spacing, maroon primary).

G) Guardrails

Do not move login back into the chat.

Do not start assessment unless: intake complete and email verified and password set.

When entering an email that already exists inside the intake, stop the sign-up path and show a login CTA only.

Preserve DEV-mode OTP echo in console + “DEV MODE” chat line. In production (SMTP available), send real emails and suppress the dev echo.

H) Small fixes

Add proper 409 response for duplicate email on sign-up.

Ensure session uses connect-pg-simple store and app.set('trust proxy', 1).

Login page submit should show inline error messages for wrong email/password (401).

Deliverables

public/login.html implemented and wired.

Updated public/index.html CTA text.

Fixed server routes (login, session/state, assessment start).

Intake email step branching as described.

Assessment error fixed by using assessService.createOrResumeSession.

No test runs—just apply code changes.